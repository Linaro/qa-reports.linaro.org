- name: Install base packages
  apt: pkg={{item}} state=installed update-cache=yes
  with_items:
    - python3
    - python3-dev
    - python-virtualenv
    - postgresql
    - libpq-dev
    - supervisor
    - apache2
    - libapache2-mod-wsgi
