- name: Install base packages
  apt: pkg={{item}} state=installed update-cache=yes
  with_items:
    - build-essential
    - python3
    - python3-dev
    - python-virtualenv
    - python-psycopg2
    - postgresql
    - libpq-dev
    - apache2
    - libapache2-mod-wsgi
